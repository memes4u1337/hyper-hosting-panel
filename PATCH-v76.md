# HYPER-HOST v76

Точечное исправление установщика Deploy Manager.

- удалена папка `ready-master-bot`;
- удалено копирование `.env.example`, `bot.py`, `requirements.txt`;
- master/template каталоги создаются пустыми;
- файлы загружаются только пользователем через отдельную страницу;
- существующие файлы не перезаписываются;
- FTP, Nginx, SSL, SQL, сайты, боты и пароль admin не изменяются.
