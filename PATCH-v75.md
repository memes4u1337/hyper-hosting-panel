# PATCH v75

- отдельная страница `/?page=deploy_center`;
- главный бот загружается тремя пользовательскими файлами: `bot.py`, `.env`, `requirements.txt`;
- файлы дочерних ботов загружаются отдельно: `bot.py`, `requirements.txt`;
- удалено автоматическое создание placeholder-файлов;
- проекты читаются из `projects` + `users` + `bot_deployments`;
- дочерний `.env` создаётся из SQL;
- каждый проект получает отдельную папку, venv и PM2;
- добавлен переписанный deploy worker с подробными Telegram-уведомлениями;
- SSL/Nginx установщик v75 не изменяет;
- пароль `admin` не изменяется.
