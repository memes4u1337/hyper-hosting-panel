diff -ruN hhbase/install.sh hhwork/install.sh
--- hhbase/install.sh	2026-07-08 11:32:50.000000000 +0000
+++ hhwork/install.sh	2026-07-09 04:43:08.918550325 +0000
@@ -275,36 +275,52 @@
 systemctl reload nginx
 
 log "Настройка FTP..."
+ensure_nologin_shell
+mkdir -p /var/run/vsftpd/empty "$FTP_DIR"
 cp /etc/vsftpd.conf "/etc/vsftpd.conf.backup.$(date +%s)" 2>/dev/null || true
+FTP_PASV_ADDR="${PUBLIC_IP:-}"
+if [[ -n "$FTP_PASV_ADDR" && "$FTP_PASV_ADDR" =~ ^(10\.|192\.168\.|172\.(1[6-9]|2[0-9]|3[0-1])\.|127\.|169\.254\.) ]]; then
+  FTP_PASV_ADDR=""
+fi
+FTP_PASV_LINE=""
+[[ -n "$FTP_PASV_ADDR" ]] && FTP_PASV_LINE="pasv_address=${FTP_PASV_ADDR}"
 cat > /etc/vsftpd.conf <<EOFTP
 listen=YES
 listen_ipv6=NO
+listen_address=0.0.0.0
 anonymous_enable=NO
 local_enable=YES
 write_enable=YES
-local_umask=022
-dirmessage_enable=YES
+local_umask=002
+file_open_mode=0664
+dirmessage_enable=NO
 use_localtime=YES
 xferlog_enable=YES
+dual_log_enable=YES
+vsftpd_log_file=/var/log/vsftpd.log
 connect_from_port_20=YES
 chroot_local_user=YES
 allow_writeable_chroot=YES
 secure_chroot_dir=/var/run/vsftpd/empty
 pam_service_name=vsftpd
+userlist_enable=NO
+tcp_wrappers=NO
 rsa_cert_file=/etc/ssl/certs/ssl-cert-snakeoil.pem
 rsa_private_key_file=/etc/ssl/private/ssl-cert-snakeoil.key
 ssl_enable=NO
 pasv_enable=YES
 pasv_min_port=40000
 pasv_max_port=40100
-pasv_address=${PUBLIC_IP:-$SERVER_IP}
+${FTP_PASV_LINE}
 pasv_addr_resolve=NO
+pasv_promiscuous=YES
+port_promiscuous=YES
 seccomp_sandbox=NO
 force_dot_files=YES
 utf8_filesystem=YES
 EOFTP
 systemctl enable vsftpd >/dev/null 2>&1 || true
-systemctl restart vsftpd
+systemctl restart vsftpd || true
 systemctl enable ssh >/dev/null 2>&1 || systemctl enable sshd >/dev/null 2>&1 || true
 systemctl restart ssh >/dev/null 2>&1 || systemctl restart sshd >/dev/null 2>&1 || true
 
@@ -354,9 +370,11 @@
     listen-on { any; };
     listen-on-v6 { any; };
     allow-query { any; };
+    allow-recursion { none; };
     recursion no;
     dnssec-validation auto;
     auth-nxdomain no;
+    minimal-responses yes;
 };
 EOBINDOPT
 touch /etc/bind/named.conf.local
diff -ruN hhbase/scripts/hhctl hhwork/scripts/hhctl
--- hhbase/scripts/hhctl	2026-07-08 15:27:40.000000000 +0000
+++ hhwork/scripts/hhctl	2026-07-09 04:44:45.866631218 +0000
@@ -711,46 +711,90 @@
   nginx_reload; log "PHP version for $domain -> $version"
 }
 
+ftp_shell_path() {
+  if [[ -x /usr/sbin/nologin ]]; then echo /usr/sbin/nologin; else echo /bin/false; fi
+}
+
 cmd_create_ftp() {
-  ensure_dirs; ensure_nologin_shell; cleanup_hyper_host_mounts
+  ensure_dirs
+  ensure_nologin_shell
+  cleanup_hyper_host_mounts
   local username="${1:-}" password="${2:-}" mode="${3:-common}"
-  [[ -n "$username" && -n "$password" ]] || err "Username and password required"; valid_name "$username" || err "Invalid ftp username"
+  [[ -n "$username" && -n "$password" ]] || err "Username and password required"
+  valid_name "$username" || err "Invalid ftp username"
   [[ "$username" == hhftp_* ]] || username="hhftp_${username}"
-  local home="$FTP_DIR/$username" common="$home/common" sites="$common/sites" bots="$common/bots"
-  mkdir -p "$common" "$sites" "$bots" "$SITES_DIR" "$BOTS_DIR"
+
+  local home="$FTP_DIR/$username" common="$home/common" sites="$common/sites" bots="$common/bots" shell
+  shell="$(ftp_shell_path)"
+
+  # Важно: home для chroot должен принадлежать root и не быть writable пользователем.
+  # Заполняем структуру до useradd без -m, чтобы не ловить ошибки из-за уже существующей папки.
+  mkdir -p "$home" "$common" "$sites" "$bots" "$SITES_DIR" "$BOTS_DIR"
   if id "$username" >/dev/null 2>&1; then
-    usermod -d "$home" -s /usr/sbin/nologin "$username" || true
-    echo "$username:$password" | chpasswd
+    usermod -d "$home" -s "$shell" "$username" || true
   else
-    useradd -m -d "$home" -s /usr/sbin/nologin "$username"
-    echo "$username:$password" | chpasswd
+    useradd -d "$home" -s "$shell" -g www-data "$username"
   fi
   usermod -aG www-data "$username" 2>/dev/null || true
-  chown root:root "$home"; chmod 0755 "$home"
-  chown -R "$username":www-data "$common"; chmod -R u+rwX,g+rwX,o+rX "$common"
-  find "$common" -type d -exec chmod g+s {} + 2>/dev/null || true
-  # В FTP пользователь видит одну общую папку common/, а внутри common/sites и common/bots.
+  echo "$username:$password" | chpasswd
+  chage -E -1 "$username" 2>/dev/null || true
+  passwd -u "$username" >/dev/null 2>&1 || true
+
+  chown root:root "$home"
+  chmod 0755 "$home"
+  chown -R "$username":www-data "$common"
+  chmod -R u+rwX,g+rwX,o+rX "$common"
+  find "$common" -type d -exec chmod 2775 {} + 2>/dev/null || true
+
+  # В FTP пользователь видит common/, внутри common/sites и common/bots — реальные папки панели.
   for pair in "$SITES_DIR:$sites" "$BOTS_DIR:$bots"; do
+    local src dst
     src="${pair%%:*}"; dst="${pair#*:}"
     mkdir -p "$src" "$dst"
     if ! mountpoint -q "$dst"; then mount --bind "$src" "$dst" 2>/dev/null || true; fi
     if mountpoint -q "$dst" && ! grep -qs " $dst " /etc/fstab; then echo "$src $dst none bind 0 0" >> /etc/fstab; fi
   done
+
   fix_web_permissions "$SITES_DIR" "$username"
   fix_web_permissions "$BOTS_DIR" "$username"
   fix_web_permissions "$common" "$username"
   ensure_vsftpd_external_access
-  log "FTP user ready: $username -> $home/common (sites/bots inside common)"
+  systemctl is-active vsftpd >/dev/null 2>&1 || err "vsftpd не запустился. Проверь: systemctl status vsftpd"
+  log "FTP user ready: $username -> $home/common (host 0.0.0.0:21, passive 40000-40100)"
 }
 cmd_delete_ftp() {
-  local username="${1:-}"; [[ -n "$username" ]] || err "Username required"; valid_name "$username" || err "Invalid username"; [[ "$username" == hhftp_* ]] || username="hhftp_${username}"
+  local username="${1:-}" tmp
+  [[ -n "$username" ]] || err "Username required"
+  valid_name "$username" || err "Invalid username"
+  [[ "$username" == hhftp_* ]] || username="hhftp_${username}"
   local home="$FTP_DIR/$username"
   for d in "$home/common/sites" "$home/common/bots" "$home/sites" "$home/bots" "$home/site"; do mountpoint -q "$d" && umount -lf "$d" || true; done
-  if [[ -f /etc/fstab ]]; then tmp="$(mktemp)"; grep -vF " $home/common/sites " /etc/fstab | grep -vF " $home/common/bots " | grep -vF " $home/sites " | grep -vF " $home/bots " | grep -vF " $home/site " > "$tmp" || true; cat "$tmp" > /etc/fstab; rm -f "$tmp"; fi
+  if [[ -f /etc/fstab ]]; then
+    tmp="$(mktemp)"
+    grep -vF " $home/common/sites " /etc/fstab | grep -vF " $home/common/bots " | grep -vF " $home/sites " | grep -vF " $home/bots " | grep -vF " $home/site " > "$tmp" || true
+    cat "$tmp" > /etc/fstab; rm -f "$tmp"
+  fi
   id "$username" >/dev/null 2>&1 && userdel "$username" || true
-  rm -rf "$home"; systemctl restart vsftpd || true; log "FTP user deleted: $username"
+  rm -rf "$home"
+  systemctl restart vsftpd || true
+  log "FTP user deleted: $username"
+}
+cmd_ftp_password() {
+  local username="${1:-}" password="${2:-}" shell
+  [[ -n "$username" && -n "$password" ]] || err "Username and password required"
+  valid_name "$username" || err "Invalid username"
+  [[ "$username" == hhftp_* ]] || username="hhftp_${username}"
+  id "$username" >/dev/null 2>&1 || err "FTP user not found"
+  shell="$(ftp_shell_path)"
+  usermod -s "$shell" "$username" 2>/dev/null || true
+  echo "$username:$password" | chpasswd
+  chage -E -1 "$username" 2>/dev/null || true
+  passwd -u "$username" >/dev/null 2>&1 || true
+  fix_web_permissions "$SITES_DIR" "$username"
+  fix_web_permissions "$BOTS_DIR" "$username"
+  ensure_vsftpd_external_access
+  log "FTP password updated: $username"
 }
-cmd_ftp_password() { local username="${1:-}" password="${2:-}"; [[ -n "$username" && -n "$password" ]] || err "Username and password required"; valid_name "$username" || err "Invalid username"; [[ "$username" == hhftp_* ]] || username="hhftp_${username}"; id "$username" >/dev/null 2>&1 || err "FTP user not found"; echo "$username:$password" | chpasswd; fix_web_permissions "$SITES_DIR" "$username"; log "FTP password updated: $username"; }
 
 cmd_create_db() {
   local db="${1:-}" user="${2:-}" pass="${3:-}" remote="${4:-0}" host_pattern="${5:-}"
@@ -1434,35 +1478,149 @@
 }
 
 cmd_dns_apply() {
-  local domain="${1:-}" records_json="${2:-[]}" ns="${3:-ns1.local.}" admin="${4:-admin.local.}"
-  valid_domain "$domain" || err "Invalid domain"; mkdir -p "$DNS_DIR"
-  local zonefile="$DNS_DIR/db.$domain" serial; serial="$(date +%Y%m%d%H)"
-  python3 - "$domain" "$records_json" "$ns" "$admin" "$zonefile" "$serial" <<'PY'
-import sys,json
-zone,records_json,ns,admin,path,serial=sys.argv[1:]
-try: records=json.loads(records_json)
-except Exception: records=[]
-with open(path,'w',encoding='utf-8') as f:
-    f.write(f"$TTL 3600\n@ IN SOA {ns} {admin} ({serial} 3600 1800 1209600 3600)\n")
-    f.write(f"@ IN NS {ns}\n")
-    for r in records:
-        name=r.get('name','@') or '@'; typ=r.get('type','A').upper(); value=r.get('value',''); ttl=int(r.get('ttl',3600) or 3600)
-        if not value: continue
-        f.write(f"{name} {ttl} IN {typ} {value}\n")
-PY
-  if [[ -d /etc/bind ]]; then
-    touch /etc/bind/named.conf.local
-    if ! grep -q "zone \"$domain\"" /etc/bind/named.conf.local; then
-      cat >> /etc/bind/named.conf.local <<EOBIND
-zone "$domain" { type master; file "$zonefile"; };
-EOBIND
-    fi
-    named-checkzone "$domain" "$zonefile" >/dev/null 2>&1 || true
+  local domain="${1:-}" records_json="${2:-[]}" ns="${3:-}" admin="${4:-}"
+  valid_domain "$domain" || err "Invalid domain"
+  mkdir -p "$DNS_DIR"
+  write_bind_authoritative_defaults
+  [[ -n "$ns" ]] || ns="ns1.$domain."
+  [[ -n "$admin" ]] || admin="admin.$domain."
+  [[ "$ns" == *. ]] || ns="$ns."
+  [[ "$admin" == *. ]] || admin="$admin."
+  local zonefile="$DNS_DIR/db.$domain" serial
+  serial="$(date +%Y%m%d%H)"
+  DOMAIN="$domain" RECORDS_JSON="$records_json" NS="$ns" ADMIN="$admin" ZONEFILE="$zonefile" SERIAL="$serial" python3 - <<'PYDNSWRITE'
+import os, json, re, ipaddress, pathlib, sys
+zone=os.environ['DOMAIN'].strip('.').lower()
+records_json=os.environ.get('RECORDS_JSON','[]')
+ns=os.environ.get('NS') or ('ns1.%s.' % zone)
+admin=os.environ.get('ADMIN') or ('admin.%s.' % zone)
+path=pathlib.Path(os.environ['ZONEFILE'])
+serial=os.environ.get('SERIAL','1')
+try:
+    records=json.loads(records_json)
+except Exception:
+    records=[]
+if not isinstance(records, list):
+    records=[]
+
+def fqdn(v):
+    v=str(v or '').strip()
+    if not v: return ''
+    return v if v.endswith('.') else v+'.'
+
+def qtxt(v):
+    v=str(v or '').strip()
+    if not v: return ''
+    if v.startswith('"') and v.endswith('"'): return v
+    return '"'+v.replace('"','\\"')+'"'
+
+def rr_name(v):
+    v=str(v or '@').strip()
+    if not v or v == zone or v == zone+'.': return '@'
+    if v.endswith('.'+zone) or v.endswith('.'+zone+'.'):
+        v=v.replace('.'+zone+'.','').replace('.'+zone,'')
+    if v.endswith('.'):
+        return fqdn(v)
+    if not re.match(r'^(\*\.)?[A-Za-z0-9_@.-]+$', v):
+        raise SystemExit('bad DNS record name: '+v)
+    return v
+
+def rr_value(typ, value):
+    value=str(value or '').strip()
+    if not value: return ''
+    typ=typ.upper()
+    if typ == 'A':
+        ipaddress.IPv4Address(value); return value
+    if typ == 'AAAA':
+        ipaddress.IPv6Address(value); return value
+    if typ in ('CNAME','NS','PTR'):
+        return fqdn(value)
+    if typ == 'MX':
+        parts=value.split()
+        if len(parts) == 1:
+            return '10 '+fqdn(parts[0])
+        return parts[0]+' '+fqdn(parts[1])
+    if typ == 'TXT':
+        return qtxt(value)
+    if typ == 'CAA':
+        return value
+    return value
+
+clean=[]
+seen=set()
+for r in records:
+    if not isinstance(r, dict):
+        continue
+    typ=str(r.get('type','A') or 'A').upper().strip()
+    if typ not in {'A','AAAA','CNAME','MX','TXT','NS','CAA','SRV'}:
+        continue
+    name=rr_name(r.get('name','@'))
+    try:
+        ttl=int(r.get('ttl',3600) or 3600)
+    except Exception:
+        ttl=3600
+    ttl=max(60, min(ttl, 86400))
+    val=rr_value(typ, r.get('value',''))
+    if not val:
+        continue
+    key=(name,typ,val)
+    if key in seen:
+        continue
+    seen.add(key)
+    clean.append((name, ttl, typ, val))
+
+# Обязательные NS для делегации домена на эту панель.
+for name, ttl, typ, val in [('@',3600,'NS','ns1.%s.'%zone),('@',3600,'NS','ns2.%s.'%zone)]:
+    key=(name,typ,val)
+    if key not in seen:
+        clean.insert(0,(name,ttl,typ,val)); seen.add(key)
+
+with path.open('w', encoding='utf-8') as f:
+    f.write('$TTL 300\n')
+    f.write('@ IN SOA %s %s (%s 300 180 1209600 300)\n' % (fqdn(ns), fqdn(admin), serial))
+    for name, ttl, typ, val in clean:
+        f.write('%s %s IN %s %s\n' % (name, ttl, typ, val))
+PYDNSWRITE
+  named-checkzone "$domain" "$zonefile" >/tmp/hyper-host-named-checkzone.log 2>&1 || { cat /tmp/hyper-host-named-checkzone.log >&2; err "DNS zone is invalid: $domain"; }
+  touch /etc/bind/named.conf.local
+  python3 - "$domain" "$zonefile" <<'PYBINDLOCAL'
+import sys,re,pathlib
+zone,zonefile=sys.argv[1:]
+p=pathlib.Path('/etc/bind/named.conf.local')
+text=p.read_text(encoding='utf-8', errors='ignore') if p.exists() else ''
+block='zone "%s" { type master; file "%s"; allow-transfer { none; }; };' % (zone, zonefile)
+pat=re.compile(r'zone\s+"%s"\s*\{[^}]*\};\s*' % re.escape(zone), re.S)
+if pat.search(text):
+    text=pat.sub(block+'\n', text)
+else:
+    text=text.rstrip()+'\n'+block+'\n'
+p.write_text(text, encoding='utf-8')
+PYBINDLOCAL
+  open_server_ports
+  systemctl enable bind9 >/dev/null 2>&1 || true
+  systemctl restart bind9 2>/dev/null || systemctl restart named 2>/dev/null || true
+  systemctl is-active bind9 >/dev/null 2>&1 || err "bind9 не запустился после применения DNS-зоны"
+  log "DNS zone applied: $domain -> $zonefile"
+}
+
+cmd_dns_delete() {
+  local domain="${1:-}"
+  valid_domain "$domain" || err "Invalid domain"
+  rm -f "$DNS_DIR/db.$domain"
+  if [[ -f /etc/bind/named.conf.local ]]; then
+    python3 - "$domain" <<'PYBINDDEL'
+import sys,re,pathlib
+zone=sys.argv[1]
+p=pathlib.Path('/etc/bind/named.conf.local')
+text=p.read_text(encoding='utf-8', errors='ignore') if p.exists() else ''
+text=re.sub(r'zone\s+"%s"\s*\{[^}]*\};\s*' % re.escape(zone), '', text, flags=re.S)
+p.write_text(text, encoding='utf-8')
+PYBINDDEL
     systemctl restart bind9 2>/dev/null || true
   fi
-  log "DNS zone applied: $domain -> $zonefile"
+  log "DNS zone deleted: $domain"
 }
-cmd_dns_delete() { local domain="${1:-}"; valid_domain "$domain" || err "Invalid domain"; rm -f "$DNS_DIR/db.$domain"; log "DNS zone file deleted: $domain"; }
+
 
 cmd_cron_set() { local name="${1:-}" schedule="${2:-}" command="${3:-}"; [[ -n "$name" && -n "$schedule" && -n "$command" ]] || err "Name schedule command required"; valid_name "$name" || err "Invalid name"; cat > "$CRON_DIR/hyper-host-task-$name" <<EOCRON
 SHELL=/bin/bash
@@ -1634,16 +1792,18 @@
 # HYPER-HOST v15: network + DNS
 # -----------------------------
 write_bind_authoritative_defaults() {
-  mkdir -p /etc/bind/hyper-host-zones
+  mkdir -p /etc/bind/hyper-host-zones /var/cache/bind
   cat > /etc/bind/named.conf.options <<'EOBINDOPT'
 options {
     directory "/var/cache/bind";
     listen-on { any; };
     listen-on-v6 { any; };
     allow-query { any; };
+    allow-recursion { none; };
     recursion no;
     dnssec-validation auto;
     auth-nxdomain no;
+    minimal-responses yes;
 };
 EOBINDOPT
   touch /etc/bind/named.conf.local
@@ -1684,44 +1844,106 @@
   fi
 }
 
+is_private_ipv4_simple() {
+  local ip="${1:-}"
+  [[ "$ip" =~ ^10\. ]] && return 0
+  [[ "$ip" =~ ^192\.168\. ]] && return 0
+  [[ "$ip" =~ ^172\.(1[6-9]|2[0-9]|3[0-1])\. ]] && return 0
+  [[ "$ip" =~ ^127\. ]] && return 0
+  [[ "$ip" =~ ^169\.254\. ]] && return 0
+  return 1
+}
+
+ftp_passive_address() {
+  local ip
+  ip="$(extract_ipv4 "$(configured_public_ip)")"
+  [[ -z "$ip" ]] && ip="$(extract_ipv4 "$(public_ip)")"
+  [[ -z "$ip" ]] && ip="$(extract_ipv4 "${PUBLIC_IP:-}")"
+  if [[ -n "$ip" ]] && ! is_private_ipv4_simple "$ip"; then
+    echo "$ip"
+  fi
+}
+
 ensure_vsftpd_external_access() {
-  # Полностью чинит FTP для подключения с другого ПК/FileZilla.
-  # Passive address должен быть публичным IP, иначе удалённый клиент получает 192.168.x.x и не заходит.
-  local pasv pub
-  pub="$(extract_ipv4 "$(configured_public_ip)")"
-  [[ -z "$pub" ]] && pub="$(extract_ipv4 "$(public_ip)")"
-  [[ -z "$pub" ]] && pub="$SERVER_IP"
-  mkdir -p /var/run/vsftpd/empty "$FTP_DIR"
+  # Полная настройка FTP: вход с любого IP на Ubuntu + passive FTP для FileZilla.
+  # Если указан публичный IP — отдаём его в PASV. Если публичного IP нет — PASV без pasv_address,
+  # чтобы локальная сеть не ломалась из-за 192.168.x.x/публичного NAT-адреса.
+  local pub pasv_line=""
+  pub="$(ftp_passive_address)"
+  [[ -n "$pub" ]] && pasv_line="pasv_address=${pub}"
+  ensure_nologin_shell
+  mkdir -p /var/run/vsftpd/empty "$FTP_DIR" /etc/vsftpd.user_conf
+  chmod 0755 /var/run/vsftpd/empty "$FTP_DIR" 2>/dev/null || true
+  cp /etc/vsftpd.conf "/etc/vsftpd.conf.backup.$(date +%s)" 2>/dev/null || true
   cat > /etc/vsftpd.conf <<EOFTP
 listen=YES
 listen_ipv6=NO
+listen_address=0.0.0.0
 anonymous_enable=NO
 local_enable=YES
 write_enable=YES
-local_umask=022
-dirmessage_enable=YES
+local_umask=002
+file_open_mode=0664
+dirmessage_enable=NO
 use_localtime=YES
 xferlog_enable=YES
+dual_log_enable=YES
+vsftpd_log_file=/var/log/vsftpd.log
 connect_from_port_20=YES
 chroot_local_user=YES
 allow_writeable_chroot=YES
 secure_chroot_dir=/var/run/vsftpd/empty
 pam_service_name=vsftpd
+userlist_enable=NO
+tcp_wrappers=NO
 rsa_cert_file=/etc/ssl/certs/ssl-cert-snakeoil.pem
 rsa_private_key_file=/etc/ssl/private/ssl-cert-snakeoil.key
 ssl_enable=NO
 pasv_enable=YES
 pasv_min_port=40000
 pasv_max_port=40100
-pasv_address=${pub}
+${pasv_line}
 pasv_addr_resolve=NO
+pasv_promiscuous=YES
+port_promiscuous=YES
 force_dot_files=YES
 utf8_filesystem=YES
 seccomp_sandbox=NO
+cmds_allowed=ABOR,CWD,DELE,LIST,MDTM,MKD,NLST,NOOP,OPTS,PASS,PASV,PORT,PWD,QUIT,RETR,RMD,RNFR,RNTO,SITE,SIZE,STOR,TYPE,USER,FEAT,SYST,EPSV,EPRT,APPE
 EOFTP
+  # На части Debian/Ubuntu pam_shells включён в /etc/pam.d/vsftpd — nologin должен быть разрешён как FTP-shell.
+  if [[ -x /usr/sbin/nologin ]] && ! grep -qxF /usr/sbin/nologin /etc/shells 2>/dev/null; then echo /usr/sbin/nologin >> /etc/shells; fi
+  if [[ -x /bin/false ]] && ! grep -qxF /bin/false /etc/shells 2>/dev/null; then echo /bin/false >> /etc/shells; fi
   open_server_ports
   systemctl enable vsftpd >/dev/null 2>&1 || true
-  systemctl restart vsftpd 2>/dev/null || true
+  systemctl restart vsftpd 2>/dev/null || systemctl start vsftpd 2>/dev/null || true
+}
+
+cmd_ftp_doctor_json() {
+  local pub pasv listen21 listen_pasv service conf users_count
+  pub="$(ftp_passive_address)"
+  pasv="$(grep -E '^pasv_address=' /etc/vsftpd.conf 2>/dev/null | tail -n1 | cut -d= -f2- || true)"
+  listen21=0; listen_port_tcp 21 && listen21=1
+  listen_pasv=0; ss -H -ltn 2>/dev/null | awk '{print $4}' | grep -Eq ':(40000|40001|40002|40003|40004|40005|40006|40007|40008|40009|40010)$' && listen_pasv=1 || true
+  service="$(systemctl is-active vsftpd 2>/dev/null || true)"
+  conf="$(test -s /etc/vsftpd.conf && echo ok || echo missing)"
+  users_count="$(getent passwd | awk -F: '$1 ~ /^hhftp_/ {c++} END{print c+0}')"
+  SERVER_IP="$SERVER_IP" PUBLIC_IP="$(public_ip)" CONFIGURED_PUBLIC_IP="$(configured_public_ip)" FTP_PASV="$pasv" FTP_PASV_EXPECTED="$pub" LISTEN21="$listen21" LISTENPASV="$listen_pasv" SERVICE="$service" CONF_TEST="$conf" USERS_COUNT="$users_count" python3 - <<'PYFTP'
+import os,json
+print(json.dumps({
+ 'server_ip': os.environ.get('SERVER_IP',''),
+ 'public_ip': os.environ.get('PUBLIC_IP',''),
+ 'configured_public_ip': os.environ.get('CONFIGURED_PUBLIC_IP',''),
+ 'vsftpd': os.environ.get('SERVICE',''),
+ 'listen_21': os.environ.get('LISTEN21') == '1',
+ 'passive_range': '40000-40100',
+ 'pasv_address': os.environ.get('FTP_PASV',''),
+ 'pasv_expected': os.environ.get('FTP_PASV_EXPECTED',''),
+ 'ftp_users': int(os.environ.get('USERS_COUNT','0') or 0),
+ 'config_test': os.environ.get('CONF_TEST',''),
+ 'client': {'protocol':'FTP', 'port':21, 'encryption':'Plain FTP', 'mode':'Passive'},
+}, ensure_ascii=False))
+PYFTP
 }
 
 cmd_access_fix() {
@@ -2148,78 +2370,92 @@
   local domain="${1:-}" pub="${2:-}" panel_sub="${3:-panel}"
   [[ -n "$domain" ]] || err "Domain required"
   valid_domain "$domain" || err "Invalid domain: $domain"
-  [[ -n "$pub" ]] || pub="$(public_ip)"
+  [[ -n "$pub" ]] || pub="$(extract_ipv4 "$(configured_public_ip)")"
+  [[ -n "$pub" ]] || pub="$(extract_ipv4 "$(public_ip)")"
   [[ "$pub" =~ ^([0-9]{1,3}\.){3}[0-9]{1,3}$ ]] || err "Invalid public IPv4: $pub"
+  [[ "$panel_sub" =~ ^[A-Za-z0-9][A-Za-z0-9-]{0,61}$ ]] || panel_sub="panel"
+  cmd_public_ip set "$pub" >/dev/null 2>&1 || true
   mkdir -p "$DNS_DIR"
   write_bind_authoritative_defaults
-  local ns1="ns1.$domain." ns2="ns2.$domain." admin="admin.$domain." records
-  records="$(python3 - <<PYREC
-import json
-pub='$pub'; panel='$panel_sub'
+  local ns1="ns1.$domain." admin="admin.$domain." records
+  records="$(DOMAIN="$domain" PUB="$pub" PANEL_SUB="$panel_sub" python3 - <<'PYREC'
+import json, os
+pub=os.environ['PUB']; panel=os.environ.get('PANEL_SUB','panel') or 'panel'
 records=[
  {'name':'@','type':'A','value':pub,'ttl':300},
  {'name':'www','type':'A','value':pub,'ttl':300},
  {'name':panel,'type':'A','value':pub,'ttl':300},
  {'name':'ns1','type':'A','value':pub,'ttl':300},
  {'name':'ns2','type':'A','value':pub,'ttl':300},
- {'name':'@','type':'MX','value':'10 mail','ttl':3600},
  {'name':'mail','type':'A','value':pub,'ttl':3600},
- {'name':'@','type':'TXT','value':'"v=spf1 a mx ip4:%s ~all"'%pub,'ttl':3600},
+ {'name':'@','type':'MX','value':'10 mail.%s.' % os.environ['DOMAIN'], 'ttl':3600},
+ {'name':'@','type':'TXT','value':'v=spf1 a mx ip4:%s ~all' % pub,'ttl':3600},
 ]
 print(json.dumps(records, ensure_ascii=False))
 PYREC
 )"
   cmd_dns_apply "$domain" "$records" "$ns1" "$admin"
-  ufw allow 53/tcp >/dev/null 2>&1 || true
-  ufw allow 53/udp >/dev/null 2>&1 || true
+  open_server_ports
   systemctl restart bind9 2>/dev/null || true
   cat <<EOF
 DNS зона готова: $domain
-
-У регистратора домена поставь NS:
-  ns1.$domain
-  ns2.$domain
-
-Если регистратор просит glue/IP для NS:
-  ns1.$domain -> $pub
-  ns2.$domain -> $pub
-
-Потом проверка:
-  dig @$pub $domain A
-  dig @$pub www.$domain A
-  sudo hyper dns status $domain
+NS: ns1.$domain / ns2.$domain
+Glue: ns1.$domain -> $pub, ns2.$domain -> $pub
+Проверка: dig @$pub $domain A
 EOF
 }
 
+
 cmd_dns_status_json() {
-  local domain="${1:-}" pub zonefile nscheck a_local a_public bind_ok named_ok open53
+  local domain="${1:-}" pub zonefile a_local a_public bind_ok named_ok udp53 tcp53 ns_local serial problem
   [[ -n "$domain" ]] || err "Domain required"
   valid_domain "$domain" || err "Invalid domain"
-  pub="$(public_ip)"; zonefile="$DNS_DIR/db.$domain"
+  pub="$(extract_ipv4 "$(configured_public_ip)")"
+  [[ -z "$pub" ]] && pub="$(extract_ipv4 "$(public_ip)")"
+  zonefile="$DNS_DIR/db.$domain"
   bind_ok=0; systemctl is-active bind9 >/dev/null 2>&1 && bind_ok=1
   named_ok=0; [[ -f "$zonefile" ]] && named-checkzone "$domain" "$zonefile" >/dev/null 2>&1 && named_ok=1
-  open53=0; ss -lun 2>/dev/null | awk '{print $5}' | grep -Eq '(:53$|0\.0\.0\.0:53|\[::\]:53)' && open53=1
+  udp53=0; listen_port_udp 53 && udp53=1
+  tcp53=0; listen_port_tcp 53 && tcp53=1
   a_local="$(dig +short @127.0.0.1 "$domain" A 2>/dev/null | sed '/^$/d' | sort -u || true)"
+  ns_local="$(dig +short @127.0.0.1 "$domain" NS 2>/dev/null | sed '/^$/d' | sort -u || true)"
   a_public="$(resolve_domain_a_public "$domain")"
-  DOMAIN="$domain" PUBLIC_IP="$pub" ZONEFILE="$zonefile" BIND_OK="$bind_ok" NAMED_OK="$named_ok" OPEN53="$open53" A_LOCAL="$a_local" A_PUBLIC="$a_public" python3 - <<'PYDNS'
+  serial="$(awk '/SOA/ {getline; print $1; exit}' "$zonefile" 2>/dev/null || true)"
+  problem=""
+  [[ "$bind_ok" == 1 ]] || problem="bind9 не запущен"
+  [[ "$named_ok" == 1 ]] || problem="zone-файл не прошёл named-checkzone"
+  [[ "$udp53" == 1 && "$tcp53" == 1 ]] || problem="порт 53 TCP/UDP не слушает"
+  DOMAIN="$domain" PUBLIC_IP="$pub" ZONEFILE="$zonefile" BIND_OK="$bind_ok" NAMED_OK="$named_ok" UDP53="$udp53" TCP53="$tcp53" A_LOCAL="$a_local" A_PUBLIC="$a_public" NS_LOCAL="$ns_local" SERIAL="$serial" PROBLEM="$problem" python3 - <<'PYDNS'
 import os,json
 lines=lambda x:[i for i in x.splitlines() if i.strip()]
+domain=os.environ['DOMAIN']
+public_ip=os.environ.get('PUBLIC_IP','')
+local_a=lines(os.environ.get('A_LOCAL',''))
+public_a=lines(os.environ.get('A_PUBLIC',''))
+ns_local=lines(os.environ.get('NS_LOCAL',''))
 print(json.dumps({
- 'domain': os.environ['DOMAIN'],
- 'public_ip': os.environ.get('PUBLIC_IP',''),
+ 'domain': domain,
+ 'public_ip': public_ip,
  'zonefile': os.environ.get('ZONEFILE',''),
  'zone_exists': os.path.exists(os.environ.get('ZONEFILE','')),
  'bind9_ok': os.environ.get('BIND_OK')=='1',
  'zone_ok': os.environ.get('NAMED_OK')=='1',
- 'listen_53_udp': os.environ.get('OPEN53')=='1',
- 'local_a': lines(os.environ.get('A_LOCAL','')),
- 'public_a': lines(os.environ.get('A_PUBLIC','')),
- 'registrar_ns_required': ['ns1.'+os.environ['DOMAIN'], 'ns2.'+os.environ['DOMAIN']],
- 'glue_required': {'ns1.'+os.environ['DOMAIN']: os.environ.get('PUBLIC_IP',''), 'ns2.'+os.environ['DOMAIN']: os.environ.get('PUBLIC_IP','')},
+ 'listen_53_udp': os.environ.get('UDP53')=='1',
+ 'listen_53_tcp': os.environ.get('TCP53')=='1',
+ 'local_a': local_a,
+ 'local_ns': ns_local,
+ 'public_a': public_a,
+ 'serial': os.environ.get('SERIAL',''),
+ 'registrar_ns_required': ['ns1.'+domain, 'ns2.'+domain],
+ 'glue_required': {'ns1.'+domain: public_ip, 'ns2.'+domain: public_ip},
+ 'ready_on_server': os.environ.get('BIND_OK')=='1' and os.environ.get('NAMED_OK')=='1' and os.environ.get('UDP53')=='1' and os.environ.get('TCP53')=='1',
+ 'delegated_publicly': bool(public_ip and public_ip in public_a),
+ 'problem': os.environ.get('PROBLEM',''),
 }, ensure_ascii=False))
 PYDNS
 }
 
+
 cmd_panel_domain() {
   local action="${1:-get}" domain="${2:-}"
   case "$action" in
@@ -2385,6 +2621,7 @@
   disk-expand) shift; cmd_disk_expand "$@" ;;
   disk-guide) shift; cmd_disk_guide "$@" ;;
   ftp-fix) shift; ensure_vsftpd_external_access; log "FTP external access fixed" ;;
+  ftp-doctor-json) shift; cmd_ftp_doctor_json ;;
   backup-run) shift; cmd_backup_run "$@" ;;
   backup-schedule) shift; cmd_backup_schedule "$@" ;;
   backup-delete-schedule) shift; cmd_backup_delete_schedule "$@" ;;
diff -ruN hhbase/scripts/hyper hhwork/scripts/hyper
--- hhbase/scripts/hyper	2026-07-08 13:17:12.000000000 +0000
+++ hhwork/scripts/hyper	2026-07-09 04:44:30.924949377 +0000
@@ -121,6 +121,7 @@
 cmd_ftp() {
   local sub="${1:-list}"
   if [[ "$sub" == "fix" ]]; then $CTL ftp-fix; return; fi
+  if [[ "$sub" == "doctor" || "$sub" == "status" ]]; then $CTL ftp-doctor-json | json_pretty; return; fi
   local json
   json="$($CTL sync-json 2>/dev/null || echo '{}')"
   JSON="$json" python3 - <<'PY'
@@ -368,6 +369,7 @@
   sudo hyper panel domain DOMAIN      привязать домен именно к панели
   sudo hyper panel ssl DOMAIN EMAIL   выпустить SSL для панели
   sudo hyper ftp                     FTP-пользователи
+  sudo hyper ftp doctor              диагностика FTP/vsftpd/passive
   sudo hyper ftp fix                 починить FTP для FileZilla
   sudo hyper access guide            гайд по подключению с любого ПК/интернета
   sudo hyper access doctor           диагностика портов SSH/FTP/HTTP/MySQL
diff -ruN hhbase/src/public/assets/style.css hhwork/src/public/assets/style.css
--- hhbase/src/public/assets/style.css	2026-07-08 10:50:05.000000000 +0000
+++ hhwork/src/public/assets/style.css	2026-07-09 04:44:18.269755273 +0000
@@ -644,3 +644,18 @@
 @media(max-width:950px){.dash-hero-v33{grid-template-columns:1fr}.dash-actions-v33{grid-template-columns:repeat(4,1fr)}.dash-action-v33{height:70px}.dash-resource-grid-v33{grid-template-columns:1fr}.dash-side-v33{grid-template-columns:1fr}}
 @media(max-width:720px){.dash-actions-v33{grid-template-columns:repeat(2,1fr)}.dash-hero-v33{padding:19px}.dash-counts-v33{grid-template-columns:1fr}}
 @media(max-width:1100px){.rail{overflow-x:auto;overflow-y:hidden}.sidebar-v2{width:100%}.flyout-head{display:none}.sidebar-status{display:none}.nav-link{min-height:44px}}
+
+/* v34 FTP/DNS hardening UI */
+.ftp-layout-v34 .compact-status{grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}
+.ftp-layout-v34 .compact-status div{padding:12px;border-radius:16px;background:rgba(255,255,255,.055);border:1px solid rgba(255,255,255,.08)}
+.ftp-connection-card{padding:16px 18px}
+.connection-line{display:flex;align-items:center;gap:12px;justify-content:space-between;flex-wrap:wrap}
+.connection-line span{color:var(--muted);font-size:13px;text-transform:uppercase;letter-spacing:.08em}
+.connection-line code{font-size:15px;color:#fff;background:rgba(255,255,255,.07);border:1px solid rgba(255,255,255,.09);padding:8px 12px;border-radius:13px}
+.connection-line b{font-size:12px;border-radius:999px;padding:7px 10px;background:rgba(84,214,126,.13);color:#9ef0b8;border:1px solid rgba(84,214,126,.22)}
+.dns-page-v34 .dns-zone-card{overflow:hidden}
+.dns-transfer-box{display:grid;grid-template-columns:1.4fr .8fr;gap:12px}
+.dns-transfer-box>div{display:flex;align-items:center;gap:8px;flex-wrap:wrap;padding:12px;border-radius:16px;background:rgba(99,102,241,.1);border:1px solid rgba(148,163,184,.18)}
+.dns-transfer-box span{color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.08em;width:100%}
+.dns-transfer-box code{background:rgba(0,0,0,.22);border:1px solid rgba(255,255,255,.08);border-radius:10px;padding:6px 8px;color:#eaf0ff}
+@media (max-width:768px){.dns-transfer-box{grid-template-columns:1fr}.connection-line{align-items:flex-start;flex-direction:column}.ftp-layout-v34 .compact-status{grid-template-columns:1fr}}
diff -ruN hhbase/src/public/index.php hhwork/src/public/index.php
--- hhbase/src/public/index.php	2026-07-08 10:50:05.000000000 +0000
+++ hhwork/src/public/index.php	2026-07-09 04:44:58.308743480 +0000
@@ -59,6 +59,40 @@
 function host_name(): string { return panel_host_for_connections(); }
 function default_ftp_password(): string { return 'Hh-' . bin2hex(random_bytes(5)) . '!'; }
 function default_db_password(): string { return 'Db-' . bin2hex(random_bytes(6)) . '!'; }
+function current_public_ipv4(): string
+{
+    $ip = trim(setting_get('public_ip_override', (string)app_config('public_ip', '')));
+    if ($ip !== '' && filter_var($ip, FILTER_VALIDATE_IP, FILTER_FLAG_IPV4)) return $ip;
+    $host = host_name();
+    return filter_var($host, FILTER_VALIDATE_IP, FILTER_FLAG_IPV4) ? $host : '';
+}
+function dns_panel_label(string $value): string
+{
+    $value = strtolower(trim($value));
+    return preg_match('/^[a-z0-9][a-z0-9-]{0,61}$/', $value) ? $value : 'panel';
+}
+function dns_default_records_for_panel(string $domain, string $ip, string $panelSub = 'panel'): array
+{
+    $panelSub = dns_panel_label($panelSub);
+    return [
+        ['name' => '@', 'type' => 'A', 'value' => $ip, 'ttl' => 300],
+        ['name' => 'www', 'type' => 'A', 'value' => $ip, 'ttl' => 300],
+        ['name' => $panelSub, 'type' => 'A', 'value' => $ip, 'ttl' => 300],
+        ['name' => 'ns1', 'type' => 'A', 'value' => $ip, 'ttl' => 300],
+        ['name' => 'ns2', 'type' => 'A', 'value' => $ip, 'ttl' => 300],
+        ['name' => 'mail', 'type' => 'A', 'value' => $ip, 'ttl' => 3600],
+        ['name' => '@', 'type' => 'MX', 'value' => '10 mail.'.$domain.'.', 'ttl' => 3600],
+        ['name' => '@', 'type' => 'TXT', 'value' => 'v=spf1 a mx ip4:'.$ip.' ~all', 'ttl' => 3600],
+    ];
+}
+function replace_dns_records(int $zoneId, array $records): void
+{
+    db()->prepare('DELETE FROM dns_records WHERE zone_id=?')->execute([$zoneId]);
+    $ins = db()->prepare('INSERT INTO dns_records(zone_id,type,name,value,ttl) VALUES(?,?,?,?,?)');
+    foreach ($records as $r) {
+        $ins->execute([$zoneId, strtoupper((string)$r['type']), (string)$r['name'], (string)$r['value'], (int)$r['ttl']]);
+    }
+}
 function back_to_current(): never { redirect($_SERVER['HTTP_REFERER'] ?? '/'); }
 
 function handle_post(string $action): void
@@ -96,6 +130,11 @@
             case 'delete_folder': {
                 $id=(int)($_POST['id']??0); $st=db()->prepare('SELECT * FROM folders WHERE id=?'); $st->execute([$id]); $f=$st->fetch(); if(!$f) throw new RuntimeException('Папка не найдена'); $res=run_ctl(['delete-folder',$f['name']],120); if($res['code']!==0) throw new RuntimeException($res['output']); db()->prepare('DELETE FROM folders WHERE id=?')->execute([$id]); redirect('/?page=sites');
             }
+            case 'ftp_fix': {
+                hh_clear_cache();
+                $res=run_ctl(['ftp-fix'],120); if($res['code']!==0) throw new RuntimeException($res['output']);
+                flash('FTP доступ исправлен: порт 21 и passive 40000-40100 подготовлены','success'); redirect('/?page=ftp');
+            }
             case 'create_ftp': {
                 $username=trim((string)($_POST['username']??'')); $password=(string)($_POST['password']??''); if($username===''||!is_valid_name($username)) throw new RuntimeException('Неверный FTP логин'); if(strlen($password)<8) throw new RuntimeException('Пароль FTP минимум 8 символов');
                 $res=run_ctl(['create-ftp',$username,$password,'all-sites'],240); if($res['code']!==0) throw new RuntimeException($res['output']); $final=str_starts_with($username,'hhftp_')?$username:'hhftp_'.$username; $home=rtrim((string)app_config('ftp_dir','/var/www/hyper-host-ftp'),'/').'/'.$final; upsert_ftp_row($final,$home,$password,host_name()); add_event('ftp','Создан FTP: '.$final); flash("FTP создан. Хост: ".host_name()." | Имя пользователя: {$final} | Пароль: {$password}",'success'); redirect('/?page=ftp');
@@ -189,7 +228,25 @@
             case 'access_fix': { hh_clear_cache(); $res=run_ctl(['access-fix'],180); if($res['code']!==0) throw new RuntimeException($res['output']); flash('Доступ с других ПК включён на Ubuntu. Роутер всё равно нужно пробросить вручную.','success'); redirect('/?page=access'); }
             case 'disk_expand': { hh_clear_cache(); $res=run_ctl(['disk-expand'],600); if($res['code']!==0) throw new RuntimeException($res['output']); flash('Диск расширен. Проверь новый размер root-раздела.','success'); redirect('/?page=disk'); }
                         case 'save_panel_domain': { $domain=strtolower(trim((string)($_POST['panel_domain']??''))); if(!is_valid_domain($domain)) throw new RuntimeException('Неверный домен панели'); $res=run_ctl(['panel-domain','set',$domain],120); if($res['code']!==0) throw new RuntimeException($res['output']); setting_set('panel_domain_override',$domain); hh_clear_cache(); flash('Домен панели сохранён: '.$domain,'success'); redirect('/?page=network'); }
-            case 'dns_wizard': { $domain=strtolower(trim((string)($_POST['domain']??''))); $ip=trim((string)($_POST['public_ip']??'')); $panel=trim((string)($_POST['panel_subdomain']??'panel')); if(!is_valid_domain($domain)) throw new RuntimeException('Неверный домен'); if($ip!=='' && !filter_var($ip,FILTER_VALIDATE_IP,FILTER_FLAG_IPV4)) throw new RuntimeException('Неверный IP'); $primary='ns1.'.$domain.'.'; $admin='admin.'.$domain.'.'; db()->prepare('INSERT INTO dns_zones(domain,primary_ns,admin_email) VALUES(?,?,?) ON CONFLICT(domain) DO UPDATE SET primary_ns=excluded.primary_ns,admin_email=excluded.admin_email')->execute([$domain,$primary,$admin]); $res=run_ctl(['dns-wizard',$domain,$ip,$panel],180); if($res['code']!==0) throw new RuntimeException($res['output']); flash("DNS-зона создана: $domain",'success'); redirect('/?page=dns'); }
+            case 'dns_wizard': {
+                $domain=strtolower(trim((string)($_POST['domain']??'')));
+                $ip=trim((string)($_POST['public_ip']??''));
+                $panel=dns_panel_label((string)($_POST['panel_subdomain']??'panel'));
+                if(!is_valid_domain($domain)) throw new RuntimeException('Неверный домен');
+                if($ip==='' ) $ip=current_public_ipv4();
+                if($ip==='' || !filter_var($ip,FILTER_VALIDATE_IP,FILTER_FLAG_IPV4)) throw new RuntimeException('Укажи публичный IPv4 для DNS');
+                $primary='ns1.'.$domain.'.'; $admin='admin.'.$domain.'.';
+                db()->beginTransaction();
+                db()->prepare('INSERT INTO dns_zones(domain,primary_ns,admin_email) VALUES(?,?,?) ON CONFLICT(domain) DO UPDATE SET primary_ns=excluded.primary_ns,admin_email=excluded.admin_email')->execute([$domain,$primary,$admin]);
+                $st=db()->prepare('SELECT id FROM dns_zones WHERE domain=?'); $st->execute([$domain]); $zoneId=(int)($st->fetch()['id']??0);
+                replace_dns_records($zoneId, dns_default_records_for_panel($domain,$ip,$panel));
+                db()->commit();
+                setting_set('public_ip_override',$ip);
+                dns_apply_zone($domain);
+                $res=run_ctl(['dns-wizard',$domain,$ip,$panel],180); if($res['code']!==0) throw new RuntimeException($res['output']);
+                hh_clear_cache();
+                flash("DNS-зона готова: $domain. NS: ns1.$domain / ns2.$domain",'success'); redirect('/?page=dns');
+            }
             case 'create_dns_zone': { $domain=strtolower(trim((string)($_POST['domain']??''))); if(!is_valid_domain($domain)) throw new RuntimeException('Неверный домен'); db()->prepare('INSERT INTO dns_zones(domain,primary_ns,admin_email) VALUES(?,?,?) ON CONFLICT(domain) DO UPDATE SET primary_ns=excluded.primary_ns,admin_email=excluded.admin_email')->execute([$domain, trim((string)($_POST['primary_ns']??'ns1.local.')), trim((string)($_POST['admin_email']??'admin.local.'))]); dns_apply_zone($domain); redirect('/?page=dns'); }
             case 'add_dns_record': { $zone=(int)($_POST['zone_id']??0); $type=strtoupper(trim((string)($_POST['type']??'A'))); $name=trim((string)($_POST['name']??'@')); $value=trim((string)($_POST['value']??'')); $ttl=(int)($_POST['ttl']??3600); if($value==='') throw new RuntimeException('Значение DNS записи пустое'); db()->prepare('INSERT INTO dns_records(zone_id,type,name,value,ttl) VALUES(?,?,?,?,?)')->execute([$zone,$type,$name,$value,$ttl]); $z=db()->prepare('SELECT domain FROM dns_zones WHERE id=?'); $z->execute([$zone]); $zr=$z->fetch(); if($zr) dns_apply_zone((string)$zr['domain']); redirect('/?page=dns'); }
             case 'delete_dns_record': { $id=(int)($_POST['id']??0); $st=db()->prepare('SELECT z.domain FROM dns_records r JOIN dns_zones z ON z.id=r.zone_id WHERE r.id=?'); $st->execute([$id]); $z=$st->fetch(); db()->prepare('DELETE FROM dns_records WHERE id=?')->execute([$id]); if($z) dns_apply_zone((string)$z['domain']); redirect('/?page=dns'); }
@@ -308,7 +365,7 @@
 function render_login(): void
 {
     $flash=flash(); $need2fa=setting_get('security_2fa_enabled','0')==='1'; ?>
-<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>HYPER-HOST</title><link rel="preconnect" href="https://cdn.jsdelivr.net"><link rel="preconnect" href="https://cdnjs.cloudflare.com" crossorigin><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet"><link href="/assets/style.css?v=33" rel="stylesheet"></head><body class="login-body">
+<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>HYPER-HOST</title><link rel="preconnect" href="https://cdn.jsdelivr.net"><link rel="preconnect" href="https://cdnjs.cloudflare.com" crossorigin><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet"><link href="/assets/style.css?v=34" rel="stylesheet"></head><body class="login-body">
 <div class="login-orb login-orb-a"></div><div class="login-orb login-orb-b"></div><div class="login-orb login-orb-c"></div>
 <div class="login-shell-v2">
   <div class="login-side">
@@ -350,7 +407,7 @@
 <link rel="preconnect" href="https://cdn.jsdelivr.net"><link rel="preconnect" href="https://cdnjs.cloudflare.com" crossorigin>
 <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
 <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet">
-<link href="/assets/style.css?v=33" rel="stylesheet"></head><body class="hh-v17"><div class="app-shell">
+<link href="/assets/style.css?v=34" rel="stylesheet"></head><body class="hh-v17"><div class="app-shell">
 <aside class="sidebar sidebar-v2">
   <div class="rail" data-active-cat="<?= e($activeCat) ?>">
     <a href="/?page=dashboard" class="rail-logo"><i class="fa-solid fa-bolt"></i></a>
@@ -380,7 +437,7 @@
     </div>
   </div>
 </aside>
-<main class="content" style="--cat-accent:<?= e($nav[$activeCat]['accent']??'#4f7dff') ?>"><header class="topbar"><div><div class="topbar-kicker"><i class="fa-solid <?= e($nav[$activeCat]['icon']??'fa-rocket') ?>"></i><?= e($nav[$activeCat]['label']??'') ?></div><h1><?= e($title) ?></h1><div class="small muted">Сервер: <code><?= e(host_name()) ?></code></div></div><form method="post"><?= csrf_field() ?><input type="hidden" name="action" value="sync_resources"><button class="btn btn-soft"><i class="fa-solid fa-rotate me-2"></i>Обновить</button></form></header><?php if($flash): ?><div class="alert alert-<?= e($flash['type']) ?> shadow-sm"><i class="fa-solid fa-circle-info me-2"></i><?= nl2br(e($flash['message'])) ?></div><?php endif; ?><?php route_view($page); ?></main></div><script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" defer></script><script src="/assets/app.js?v=33" defer></script></body></html><?php
+<main class="content" style="--cat-accent:<?= e($nav[$activeCat]['accent']??'#4f7dff') ?>"><header class="topbar"><div><div class="topbar-kicker"><i class="fa-solid <?= e($nav[$activeCat]['icon']??'fa-rocket') ?>"></i><?= e($nav[$activeCat]['label']??'') ?></div><h1><?= e($title) ?></h1><div class="small muted">Сервер: <code><?= e(host_name()) ?></code></div></div><form method="post"><?= csrf_field() ?><input type="hidden" name="action" value="sync_resources"><button class="btn btn-soft"><i class="fa-solid fa-rotate me-2"></i>Обновить</button></form></header><?php if($flash): ?><div class="alert alert-<?= e($flash['type']) ?> shadow-sm"><i class="fa-solid fa-circle-info me-2"></i><?= nl2br(e($flash['message'])) ?></div><?php endif; ?><?php route_view($page); ?></main></div><script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" defer></script><script src="/assets/app.js?v=34" defer></script></body></html><?php
 }
 function route_view(string $page): void { match($page){ 'files'=>view_files(), 'sites'=>view_sites(), 'ftp'=>view_ftp(), 'databases'=>view_databases(), 'pma_login'=>view_pma_login(), 'bots'=>view_bots(), 'bot_logs'=>view_bot_logs(), 'backups'=>view_backups(), 'dns'=>view_dns(), 'network'=>view_network(), 'ssl'=>view_ssl(), 'php'=>view_php(), 'cron'=>view_cron(), 'logs'=>view_logs(), 'security'=>view_security(), 'settings'=>view_settings(), 'access'=>view_access(), 'disk'=>view_disk(), default=>view_dashboard(), }; }
 function stat_card(string $icon,string $label,string $value,string $sub=''): void { ?><div class="stat-card"><div class="stat-icon"><i class="fa-solid <?= e($icon) ?>"></i></div><div><span><?= e($label) ?></span><b><?= e($value) ?></b><?php if($sub): ?><em><?= e($sub) ?></em><?php endif; ?></div></div><?php }
@@ -686,32 +743,41 @@
 }
 
 function view_ftp(): void
-{ $rows=db()->query('SELECT * FROM ftp_accounts ORDER BY id DESC')->fetchAll(); $gen=default_ftp_password(); ?>
-<div class="ftp-layout-v33 row g-4">
+{ $rows=db()->query('SELECT * FROM ftp_accounts ORDER BY id DESC')->fetchAll(); $gen=default_ftp_password(); $doctor=run_ctl_json_cached(['ftp-doctor-json'],8,30); $ftpHost=setting_get('public_ip_override', host_name()); ?>
+<div class="ftp-layout-v34 row g-4">
   <div class="col-lg-4">
     <div class="panel-card ftp-create-card">
       <div class="kicker"><i class="fa-solid fa-folder-tree me-2"></i>FTP</div>
-      <h2>Создать аккаунт</h2>
+      <h2>Аккаунты доступа</h2>
+      <div class="status-grid compact-status mt-3">
+        <div><span>vsftpd</span><b class="<?= (($doctor['vsftpd']??'')==='active')?'text-success':'text-danger' ?>"><?= e((string)($doctor['vsftpd']??'unknown')) ?></b></div>
+        <div><span>порт</span><b class="<?= !empty($doctor['listen_21'])?'text-success':'text-danger' ?>"><?= !empty($doctor['listen_21'])?'21 открыт':'21 закрыт' ?></b></div>
+        <div><span>passive</span><b>40000-40100</b></div>
+        <div><span>PASV IP</span><b><?= e((string)(($doctor['pasv_address'] ?? '') ?: 'auto')) ?></b></div>
+      </div>
       <form method="post" class="vstack gap-3 mt-3"><?= csrf_field() ?><input type="hidden" name="action" value="create_ftp">
         <input class="form-control" name="username" placeholder="hyperhost" required>
         <div class="input-group"><input class="form-control" name="password" id="ftpPass" value="<?= e($gen) ?>" minlength="8" required><button class="btn btn-outline-light" type="button" onclick="copyValue('ftpPass')"><i class="fa-regular fa-copy"></i></button></div>
         <button class="btn btn-primary btn-lg"><i class="fa-solid fa-plus me-2"></i>Создать FTP</button>
       </form>
-      <form method="post" class="mt-3"><?= csrf_field() ?><input type="hidden" name="action" value="repair_panel"><button class="btn btn-soft w-100"><i class="fa-solid fa-screwdriver-wrench me-2"></i>Починить доступ</button></form>
+      <form method="post" class="mt-3"><?= csrf_field() ?><input type="hidden" name="action" value="ftp_fix"><button class="btn btn-soft w-100"><i class="fa-solid fa-screwdriver-wrench me-2"></i>Починить FTP</button></form>
     </div>
   </div>
   <div class="col-lg-8">
+    <div class="panel-card mb-3 ftp-connection-card">
+      <div class="connection-line"><span>Подключение</span><code>FTP <?= e($ftpHost) ?> : 21</code><b>Passive</b></div>
+    </div>
     <div class="row g-3">
-      <?php foreach($rows as $r): ?>
+      <?php foreach($rows as $r): $cardHost=setting_get('public_ip_override', $r['host']?:host_name()); ?>
         <div class="col-md-6"><div class="ftp-card ftp-card-v25">
           <h3><i class="fa-solid fa-user-lock me-2"></i><?= e($r['username']) ?></h3>
-          <div class="cred"><span>Хост</span><code><?= e(setting_get('public_ip_override', $r['host']?:host_name())) ?></code></div>
-          <div class="cred"><span>Локальный</span><code><?= e(host_name()) ?></code></div>
+          <div class="cred"><span>Хост</span><code><?= e($cardHost) ?></code></div>
+          <div class="cred"><span>Порт</span><code>21</code></div>
           <div class="cred"><span>Логин</span><code><?= e($r['username']) ?></code></div>
           <div class="cred"><span>Пароль</span><code><?= e($r['password_plain']?:'задать новый') ?></code></div>
-          <div class="ftp-tags"><span>21</span><span>40000-40100</span><span>common/sites</span></div>
+          <div class="ftp-tags"><span>FTP</span><span>Passive</span><span>common/sites</span></div>
           <div class="d-flex gap-2 mt-3 flex-wrap">
-            <button class="btn btn-sm btn-light" onclick="copyText('Host: <?= e(setting_get('public_ip_override', $r['host']?:host_name())) ?>\nLogin: <?= e($r['username']) ?>\nPassword: <?= e($r['password_plain']) ?>\nPort: 21\nPassive: 40000-40100')">Копировать</button>
+            <button class="btn btn-sm btn-light" onclick="copyText('Protocol: FTP\nHost: <?= e($cardHost) ?>\nPort: 21\nMode: Passive\nLogin: <?= e($r['username']) ?>\nPassword: <?= e($r['password_plain']) ?>')">Копировать</button>
             <button class="btn btn-sm btn-outline-light" data-bs-toggle="modal" data-bs-target="#ftp<?= (int)$r['id'] ?>">Пароль</button>
             <form method="post" onsubmit="return confirm('Удалить FTP?')"><?= csrf_field() ?><input type="hidden" name="action" value="delete_ftp"><input type="hidden" name="id" value="<?= (int)$r['id'] ?>"><button class="btn btn-sm btn-danger">Удалить</button></form>
           </div>
@@ -722,6 +788,7 @@
   </div>
 </div><?php }
 
+
 function pm2_status_map(): array { $d=run_ctl_json_live(['bot-list-json'],8); $m=[]; if(!isset($d['_error'])) foreach($d as $p) $m[(string)$p['name']]=$p; return $m; }
 function view_bots(): void
 {
@@ -813,36 +880,41 @@
 <div class="row g-4"><div class="col-lg-4"><div class="panel-card"><h2>Создать backup сейчас</h2><form method="post" class="vstack gap-3"><?= csrf_field() ?><input type="hidden" name="action" value="backup_run"><select class="form-select" name="target"><option value="all">Всё</option><option value="sites">Сайты</option><option value="bots">Боты</option><option value="db">Базы MySQL</option><option value="panel">Панель</option></select><button class="btn btn-primary">Создать backup</button></form></div><div class="panel-card mt-4"><h2>Расписание</h2><form method="post" class="vstack gap-3"><?= csrf_field() ?><input type="hidden" name="action" value="backup_job"><input class="form-control" name="name" placeholder="daily_all" required><input class="form-control" name="schedule" value="0 3 * * *" required><select class="form-select" name="target"><option value="all">Всё</option><option value="sites">Сайты</option><option value="bots">Боты</option><option value="db">Базы</option></select><button class="btn btn-primary">Сохранить расписание</button></form></div></div><div class="col-lg-8"><div class="panel-card"><h2>Backup задачи</h2><table class="table table-dark-soft"><?php foreach($jobs as $j): ?><tr><td><b><?= e($j['name']) ?></b></td><td><code><?= e($j['schedule']) ?></code></td><td><?= e($j['target']) ?></td><td><form method="post"><?= csrf_field() ?><input type="hidden" name="action" value="delete_backup_job"><input type="hidden" name="id" value="<?= (int)$j['id'] ?>"><button class="btn btn-sm btn-outline-danger">Удалить</button></form></td></tr><?php endforeach; if(!$jobs): ?><tr><td class="empty">Задач пока нет</td></tr><?php endif; ?></table><h2 class="mt-4">Архивы</h2><table class="table table-dark-soft"><?php foreach(($files['_error']??null)?[]:$files as $f): ?><tr><td><code><?= e($f['name']) ?></code></td><td><?= e(human_bytes((float)$f['size'])) ?></td><td><?= e(date('d.m.Y H:i',(int)$f['mtime'])) ?></td></tr><?php endforeach; ?></table></div></div></div><?php }
 
 function view_dns(): void { $zones=db()->query('SELECT * FROM dns_zones ORDER BY id DESC')->fetchAll(); $pub=setting_get('public_ip_override',(string)app_config('public_ip','90.189.208.25')); ?>
-<div class="row g-4">
+<div class="row g-4 dns-page-v34">
   <div class="col-xl-4">
     <div class="panel-card hero-mini">
       <div class="kicker"><i class="fa-solid fa-diagram-project me-2"></i>DNS</div>
-      <h2>DNS-зона</h2>
+      <h2>Перенос домена</h2>
       <form method="post" class="vstack gap-3 mt-3"><?= csrf_field() ?><input type="hidden" name="action" value="dns_wizard">
         <input class="form-control" name="domain" placeholder="hyper-host.pw" required>
         <input class="form-control" name="public_ip" value="<?= e($pub) ?>" placeholder="90.189.208.25">
         <input class="form-control" name="panel_subdomain" value="panel" placeholder="panel">
-        <button class="btn btn-primary btn-lg"><i class="fa-solid fa-wand-magic-sparkles me-2"></i>Создать DNS</button>
+        <button class="btn btn-primary btn-lg"><i class="fa-solid fa-wand-magic-sparkles me-2"></i>Создать зону</button>
       </form>
     </div>
   </div>
   <div class="col-xl-8">
-    <?php foreach($zones as $z): $rs=db()->prepare('SELECT * FROM dns_records WHERE zone_id=? ORDER BY id'); $rs->execute([(int)$z['id']]); $recs=$rs->fetchAll(); $status=run_ctl_json_cached(['dns-status-json',$z['domain']],8,180); ?>
-      <div class="panel-card mb-4">
-        <div class="card-title-row align-items-start"><div><h2><?= e($z['domain']) ?></h2><div class="small muted">NS: <code>ns1.<?= e($z['domain']) ?></code> / <code>ns2.<?= e($z['domain']) ?></code></div></div><form method="post" onsubmit="return confirm('Удалить DNS зону?')"><?= csrf_field() ?><input type="hidden" name="action" value="delete_dns_zone"><input type="hidden" name="id" value="<?= (int)$z['id'] ?>"><button class="btn btn-sm btn-outline-danger">Удалить</button></form></div>
+    <?php foreach($zones as $z): $rs=db()->prepare('SELECT * FROM dns_records WHERE zone_id=? ORDER BY id'); $rs->execute([(int)$z['id']]); $recs=$rs->fetchAll(); $status=run_ctl_json_cached(['dns-status-json',$z['domain']],8,60); ?>
+      <div class="panel-card mb-4 dns-zone-card">
+        <div class="card-title-row align-items-start"><div><h2><?= e($z['domain']) ?></h2><div class="small muted"><code>ns1.<?= e($z['domain']) ?></code> / <code>ns2.<?= e($z['domain']) ?></code> → <code><?= e((string)($status['public_ip'] ?? $pub)) ?></code></div></div><form method="post" onsubmit="return confirm('Удалить DNS зону?')"><?= csrf_field() ?><input type="hidden" name="action" value="delete_dns_zone"><input type="hidden" name="id" value="<?= (int)$z['id'] ?>"><button class="btn btn-sm btn-outline-danger">Удалить</button></form></div>
         <div class="status-grid mb-3">
-          <div><span>Bind9</span><b class="<?= !empty($status['bind9_ok'])?'text-success':'text-danger' ?>"><?= !empty($status['bind9_ok'])?'работает':'ошибка' ?></b></div>
+          <div><span>Bind9</span><b class="<?= !empty($status['bind9_ok'])?'text-success':'text-danger' ?>"><?= !empty($status['bind9_ok'])?'active':'error' ?></b></div>
           <div><span>Zone</span><b class="<?= !empty($status['zone_ok'])?'text-success':'text-danger' ?>"><?= !empty($status['zone_ok'])?'ok':'bad' ?></b></div>
-          <div><span>53 UDP</span><b class="<?= !empty($status['listen_53_udp'])?'text-success':'text-warning' ?>"><?= !empty($status['listen_53_udp'])?'слушает':'нет' ?></b></div>
-          <div><span>Public A</span><b><?= e(implode(', ', $status['public_a'] ?? [])) ?: 'ещё не делегирован' ?></b></div>
+          <div><span>53 UDP/TCP</span><b class="<?= (!empty($status['listen_53_udp'])&&!empty($status['listen_53_tcp']))?'text-success':'text-warning' ?>"><?= (!empty($status['listen_53_udp'])&&!empty($status['listen_53_tcp']))?'open':'closed' ?></b></div>
+          <div><span>Public A</span><b><?= e(implode(', ', $status['public_a'] ?? [])) ?: 'не делегирован' ?></b></div>
         </div>
-        <form method="post" class="row g-2 mb-3"><?= csrf_field() ?><input type="hidden" name="action" value="add_dns_record"><input type="hidden" name="zone_id" value="<?= (int)$z['id'] ?>"><div class="col-md-2"><select class="form-select" name="type"><option>A</option><option>AAAA</option><option>CNAME</option><option>MX</option><option>TXT</option><option>NS</option></select></div><div class="col-md-2"><input class="form-control" name="name" value="@"></div><div class="col-md-5"><input class="form-control" name="value" placeholder="IP / value"></div><div class="col-md-2"><input class="form-control" name="ttl" value="300"></div><div class="col-md-1"><button class="btn btn-primary w-100">+</button></div></form>
+        <div class="dns-transfer-box mb-3">
+          <div><span>NS у регистратора</span><code>ns1.<?= e($z['domain']) ?></code><code>ns2.<?= e($z['domain']) ?></code></div>
+          <div><span>Glue IP</span><code><?= e((string)($status['public_ip'] ?? $pub)) ?></code></div>
+        </div>
+        <form method="post" class="row g-2 mb-3"><?= csrf_field() ?><input type="hidden" name="action" value="add_dns_record"><input type="hidden" name="zone_id" value="<?= (int)$z['id'] ?>"><div class="col-md-2"><select class="form-select" name="type"><option>A</option><option>AAAA</option><option>CNAME</option><option>MX</option><option>TXT</option><option>NS</option><option>CAA</option></select></div><div class="col-md-2"><input class="form-control" name="name" value="@"></div><div class="col-md-5"><input class="form-control" name="value" placeholder="IP / value"></div><div class="col-md-2"><input class="form-control" name="ttl" value="300"></div><div class="col-md-1"><button class="btn btn-primary w-100">+</button></div></form>
         <div class="table-responsive"><table class="table table-dark-soft align-middle"><thead><tr><th>Тип</th><th>Имя</th><th>Значение</th><th>TTL</th><th></th></tr></thead><tbody><?php foreach($recs as $r): ?><tr><td><span class="badge text-bg-secondary"><?= e($r['type']) ?></span></td><td><?= e($r['name']) ?></td><td><code><?= e($r['value']) ?></code></td><td><?= (int)$r['ttl'] ?></td><td class="text-end"><form method="post"><?= csrf_field() ?><input type="hidden" name="action" value="delete_dns_record"><input type="hidden" name="id" value="<?= (int)$r['id'] ?>"><button class="btn btn-sm btn-outline-danger">x</button></form></td></tr><?php endforeach; ?></tbody></table></div>
       </div>
     <?php endforeach; if(!$zones): ?><div class="panel-card empty">DNS зон пока нет</div><?php endif; ?>
   </div>
 </div><?php }
 
+
 function view_network(): void { $sites=db()->query('SELECT * FROM sites ORDER BY domain')->fetchAll(); $domain=(string)($_GET['domain']??($sites[0]['domain']??'hyper-host.pw')); $pub=setting_get('public_ip_override',(string)app_config('public_ip','90.189.208.25')); $doctor=run_ctl_json_cached(['network-doctor-json',$domain],8,180); ?>
 <div class="row g-4">
   <div class="col-xl-5"><div class="panel-card hero-mini"><div class="kicker"><i class="fa-solid fa-tower-broadcast me-2"></i>Сеть</div><h2>Внешний доступ</h2>
