# HYPER-HOST v86

Загрузите содержимое этой папки в корень ветки `main`, затем выполните:

```bash
cd /tmp && sudo rm -rf hyper-host-update && git clone --depth 1 --branch main https://github.com/memes4u1337/hyper-hosting-panel.git hyper-host-update && cd hyper-host-update && sudo bash apply-v86-final-routing-panel-ssl-recovery.sh beta.mystockbot.xyz
```

Отчёт:

```bash
sudo cat /root/hyper-host-v86-final-routing-panel-ssl-report.txt
sudo cat /opt/hyper-host/data/v86-routing.tsv
sudo cat /opt/hyper-host/data/v86-nginx-cleanup.txt
```
