# HYPER-HOST v87

Установка:

```bash
sudo bash apply-v87-lan-bound-final-recovery.sh beta.mystockbot.xyz
```

Отчёт:

```bash
sudo cat /root/hyper-host-v87-lan-bound-final-report.txt
sudo cat /opt/hyper-host/data/v87-routing.tsv
sudo cat /opt/hyper-host/data/v87-nginx-cleanup.txt
```
